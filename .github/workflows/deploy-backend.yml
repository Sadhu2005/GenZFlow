# Backend CI/CD - Use Render Auto-Deploy Instead
# This workflow is disabled in favor of Render's built-in GitHub integration
# 
# To enable automatic deployment:
# 1. Go to Render.com dashboard
# 2. Select your backend service
# 3. Go to Settings → Build & Deploy
# 4. Enable "Auto-Deploy" and set branch to "main"
# 5. Save settings
#
# This is much simpler and more reliable than GitHub Actions for Render deployment

name: Backend CI/CD (Use Render Auto-Deploy)

on:
  workflow_dispatch: # Manual trigger only

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
    - name: Render Auto-Deploy Info
      run: |
        echo "🚀 Backend CI/CD Setup Instructions:"
        echo ""
        echo "1. Go to Render.com dashboard"
        echo "2. Select your backend service (genzflow-backend)"
        echo "3. Go to Settings → Build & Deploy"
        echo "4. Enable 'Auto-Deploy'"
        echo "5. Set branch to 'main'"
        echo "6. Save settings"
        echo ""
        echo "✅ After setup, every push to main will auto-deploy!"
        echo "🔧 Backend URL: https://genzflow-backend.onrender.com"
        echo ""
        echo "📋 Benefits of Render Auto-Deploy:"
        echo "   - Simpler setup (no GitHub secrets needed)"
        echo "   - More reliable than GitHub Actions"
        echo "   - Built-in build logs and monitoring"
        echo "   - Automatic rollback on failure"
